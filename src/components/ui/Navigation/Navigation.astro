<header class="absolute inset-x-0 top-0 z-50">
  <nav aria-label="Global" class="flex items-center justify-between p-6 lg:px-8">
    <!-- Logo -->
    <div class="flex lg:flex-1">
      <a href="/" class="-m-1.5 p-1.5">
        <span class="sr-only">MUGA</span>
        <img src="./favicon.svg" alt="" class="h-8 w-auto" />
      </a>
    </div>

    <!-- Botón menú móvil -->
    <div class="flex lg:hidden">
      <a href="#mobile-menu" class="-m-2.5 inline-flex items-center justify-center rounded-md p-2.5 text-gray-200">
        <span class="sr-only">Open main menu</span>
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" class="size-6" aria-hidden="true">
          <path d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>

    <!-- Links desktop -->
    <div class="hidden lg:flex lg:gap-x-12">
      <a href="#" class="text-sm font-semibold text-white">Productos</a>
      <a href="#" class="text-sm font-semibold text-white">Características</a>
      <a href="#" class="text-sm font-semibold text-white">Nosotros</a>
      <a href="#" class="text-sm font-semibold text-white">Contacto</a>
    </div>

    <div class="hidden lg:flex lg:flex-1 lg:justify-end">
      <a href="#" class="text-sm font-semibold text-white">Portafolio <span aria-hidden="true">&rarr;</span></a>
    </div>
  </nav>

  <!-- Overlay menú mobile con :target -->
  <div id="mobile-menu" class="fixed inset-0 z-50 hidden lg:hidden bg-zinc-900/95">
    <div class="fixed inset-y-0 right-0 w-full sm:max-w-sm p-6 overflow-y-auto bg-zinc-900 sm:ring-1 sm:ring-gray-100/10">
      <!-- Header del menú -->
      <div class="flex items-center justify-between">
        <a href="/" class="-m-1.5 p-1.5">
          <span class="sr-only">MUGA</span>
          <img src="./favicon.svg" alt="" class="h-8 w-auto" />
        </a>
        <a href="#" class="-m-2.5 rounded-md p-2.5 text-gray-200">
          <span class="sr-only">Close menu</span>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" class="size-6" aria-hidden="true">
            <path d="M6 18 18 6M6 6l12 12" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>

      <!-- Links -->
      <div class="mt-6 flow-root">
        <div class="-my-6 divide-y divide-white/10">
          <div class="space-y-2 py-6">
            <a href="#" class="block rounded-lg px-3 py-2 text-base font-semibold text-white hover:bg-white/5">Productos</a>
            <a href="#" class="block rounded-lg px-3 py-2 text-base font-semibold text-white hover:bg-white/5">Características</a>
            <a href="#" class="block rounded-lg px-3 py-2 text-base font-semibold text-white hover:bg-white/5">Nosotros</a>
          </div>
          <div class="py-6">
            <a href="#" class="block rounded-lg px-3 py-2.5 text-base font-semibold text-white hover:bg-white/5">Portafolio</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>

<style>
  /* Truco con :target para abrir/cerrar */
  #mobile-menu:target {
    display: block;
  }
</style>
